non_aligned_movement_decisions = {
	
	join_non_aligned_movement = {
		visible = {
			NOT = { has_idea = non_aligned_movement_member }
			NOT = { is_in_faction_with = USA }
			NOT = { is_in_faction_with = SOV }
			NOT = { is_in_faction_with = PRC }
		}
		available = {
			NOT = { is_in_faction_with = USA }
			NOT = { is_in_faction_with = SOV }
			NOT = { is_in_faction_with = PRC }
		}
		
		complete_effect = {
            add_ideas = non_aligned_movement_member
		}
		
		icon = politics
		
		cost = 100
		
		ai_will_do = {
			base = 0
		}
	}
	
	leave_non_aligned_movement = {
		visible = {
			has_idea = non_aligned_movement_member
		}
		available = {
            always = yes
		}
		
		complete_effect = {
            remove_ideas = non_aligned_movement_member
		}
		
		icon = politics
		
		cost = 50
		
		ai_will_do = {
			base = 0
		}
	}
}

baltic_assembly_decisions = {
	
	rejoin_baltic_assembly = {
		visible = {
			NOT = { has_idea = baltic_assembly_member }
			OR = { original_tag = EST original_tag = LAT original_tag = LIT	}
		}
		available = {
            NOT = { has_idea = baltic_assembly_member }
		}
		
		complete_effect = {
            add_ideas = baltic_assembly_member
		}
		
		icon = politics
		
		cost = 100
		
		ai_will_do = {
			base = 0
		}
	}
	
	leave_baltic_assembly = {
		visible = {
			has_idea = baltic_assembly_member
		}
		available = {
            always = yes
		}
		
		complete_effect = {
            remove_ideas = baltic_assembly_member
		}
		
		icon = politics
		
		cost = 50
		
		ai_will_do = {
			base = 0
		}
	}
}

nordic_council_decisions = {
	
	rejoin_nordic_council = {
		visible = {
			NOT = { has_idea = nordic_council_member }
			OR = { original_tag = SWE original_tag = DEN original_tag = NOR original_tag = FIN original_tag = ICE }
		}
		available = {
            NOT = { has_idea = nordic_council_member }
		}
		
		complete_effect = {
            add_ideas = nordic_council_member
		}
		
		icon = politics
		
		cost = 100
		
		ai_will_do = {
			base = 0
		}
	}
	
	leave_nordic_council = {
		visible = {
			has_idea = nordic_council_member
		}
		available = {
            always = yes
		}
		
		complete_effect = {
            remove_ideas = nordic_council_member
		}
		
		icon = politics
		
		cost = 50
		
		ai_will_do = {
			base = 0
		}
	}
}

east_african_community_decisions = {
	
	rejoin_east_african_community = {
		visible = {
			NOT = { has_idea = east_african_community_member }
			OR = { original_tag = SSD original_tag = UGA original_tag = RWA original_tag = KEN original_tag = TNZ original_tag = BUR }
		}
		available = {
            NOT = { has_idea = east_african_community_member }
		}
		
		complete_effect = {
            add_ideas = east_african_community_member
		}
		
		icon = politics
		
		cost = 100
		
		ai_will_do = {
			base = 0
		}
	}
	
	leave_east_african_community = {
		visible = {
			has_idea = east_african_community_member
		}
		available = {
            always = yes
		}
		
		complete_effect = {
            remove_ideas = east_african_community_member
		}
		
		icon = politics
		
		cost = 50
		
		ai_will_do = {
			base = 0
		}
	}
}