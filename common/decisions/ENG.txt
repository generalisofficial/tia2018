decisions_ENG = {
	falkland_policy = {
		visible = {
			country_exists = ARG
			NOT = { has_country_flag = falkland_policy }
		}
		
		available = {
			NOT = { has_war_with = ARG }
			is_subject = no
		}
		
		icon = falkland_islands
		
		cost = 5
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = falkland_policy
			country_event = britain_decision.1
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				date > 2000.6.1
				add = 5
			}
		}
	}
}

brexit_category = {
	hold_second_referendum = {
		visible = {
		    NOT = { has_global_flag = brexit }
		}
		
		available = {
		    NOT = { has_global_flag = brexit }
			has_political_power > 150
		}
		
		icon = brexit
		
		cost = 150
		fire_only_once = yes
		
		complete_effect = {
            set_global_flag = placeholder
		}
		
		ai_will_do = {
			base = 0
		}
	}
}