add_namespace = money

country_event = {
	id = economy.1
	hidden = yes

	is_triggered_only = yes

	immediate = {
		economy_setup = yes
		add_to_array = {
			array = global.xcountry_grid_array
			value = ROOT.id
		}
		money_update_expenses_scripted_effect = yes
	}
}

country_event = {
	id = economy.2
	hidden = yes

	is_triggered_only = yes

	immediate = {
		every_country = {
        	monthly_economy_pulse = yes
        }
	}
}